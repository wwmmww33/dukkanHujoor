<main class="main-content">
    <div class="auth-container fade-in">
        <div class="form-card" style="max-width: 500px;">
            <div class="form-header">
                <h1 class="form-title">🔑 استعادة كلمة المرور</h1>
                <p class="form-subtitle">أدخل بريدك الإلكتروني المسجل وسنرسل لك رابطاً لإعادة التعيين.</p>
            </div>
            
            <% if (error) { %>
                <div class="alert alert-error"><%= error %></div>
            <% } %>
            <% if (success) { %>
                <div class="alert alert-success"><%= success %></div>
            <% } %>
            
            <form method="POST" action="/forgot-password">
                <div class="form-group">
                    <label for="email" class="form-label">📧 البريد الإلكتروني</label>
                    <input type="email" id="email" name="email" required class="form-input" placeholder="example@email.com">
                </div>
                
                <!-- تم تطبيق الكلاس الموحد هنا -->
                <button type="submit" class="submit-btn">إرسال رابط الاستعادة</button>
            </form>
             <div class="auth-footer" style="margin-top: 1.5rem; padding-top: 1rem; border-top: 1px solid var(--border-color);">
                <a href="/login" class="auth-link">العودة لتسجيل الدخول</a>
            </div>
        </div>
    </div>
</main>